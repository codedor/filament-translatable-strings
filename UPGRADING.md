# Upgrading

## From v1 to v2

- Install `wotz/filament-translatable-strings` instead of `codedor/filament-translatable-strings`
- Replace all occurrences of `Codedor\TranslatableStrings` namespace with new `Wotz\TranslatableStrings` namespace
